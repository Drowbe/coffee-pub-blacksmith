========================================
JOURNAL IMAGE — CORE (PORTRAIT COPY)
========================================

Shared expectations for **Portrait Image** copy (composed with the portrait profile below). Illustration Image uses its own self-contained prompt.

ACTIVE TASK
Generate **one** portrait image now. Apply PREFILLED FACETS and STYLE RULES in the portrait profile.

- **Do not** render prompt text inside the image.
- **Do not** ask "what image?" when facets are present.

SCOPE
- **Image-only.** Does **not** produce JSON.

OUTPUT
- Generate **one** image now unless the user explicitly asks for more.
- Use your **image-generation tool** when available.

FAILURE
If image generation fails, output exactly:
Image generation failed
