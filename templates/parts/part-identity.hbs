{{!-- Identity chip: avatar, primary name, secondary line. Not actor-specific --
      the reference cards use it for a GM, a character, and a player alike. --}}
<div class="container-user">
    {{#if img}}<img class="token-image" src="{{img}}" alt="" />{{/if}}
    <div class="token-text-wrapper">
        <span class="token-name">{{name}}</span>
        {{#if subtitle}}<span class="token-character">{{subtitle}}</span>{{/if}}
    </div>
</div>
