{{!-- Standalone state chips. --}}
<div class="blacksmith-part-badges">
    {{#each items}}
    <span class="blacksmith-part-badge">
        {{#if this.icon}}<i class="{{this.icon}}"></i>{{/if}}{{{this.label}}}
    </span>
    {{/each}}
</div>
