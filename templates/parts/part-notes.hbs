{{!-- Footer notes: icon and text. --}}
<div class="blacksmith-part-notes">
    {{#each items}}
    <div class="blacksmith-part-note">
        {{#if this.icon}}<i class="{{this.icon}}"></i>{{/if}}<span>{{{this.label}}}</span>
    </div>
    {{/each}}
</div>
