{{!-- Discrete state slots, optionally split around a centre marker.

      Death saves are one use; hit dice, charges, ammunition, and legendary
      actions are the same shape. With a centre marker, the leading group is
      right-aligned and the trailing group left-aligned, so both fill outward
      from the centre -- the marker is the pivot, which is how Crier's widget has
      always read.

      The centre is the click target when an action is supplied; individual pips
      stay display-only. --}}
<div class="blacksmith-part-pips{{#if center}} blacksmith-pips-centred{{/if}}">

    {{#if leading}}
    <div class="blacksmith-part-pip-group blacksmith-pip-lead blacksmith-pip-tone-{{leading.tone}}">
        {{#each leading.pips}}
        <span class="blacksmith-part-pip{{#if this}} blacksmith-pip-filled{{/if}}"></span>
        {{/each}}
    </div>
    {{/if}}

    {{#if center}}
    <div class="blacksmith-part-pip-center">
        {{#if center.action}}
        <button type="button" class="blacksmith-part-pip-button"
                data-blacksmith-module="{{center.moduleId}}" data-blacksmith-action="{{center.action}}"
                {{#if center.value}}data-blacksmith-value="{{center.value}}"{{/if}}
                {{#if center.tooltip}}data-tooltip="{{center.tooltip}}"{{/if}}>
            <i class="{{center.icon}}{{#if center.animation}} blacksmith-anim-{{center.animation}}{{/if}}"></i>
        </button>
        {{else}}
        <i class="{{center.icon}}{{#if center.animation}} blacksmith-anim-{{center.animation}}{{/if}}"
           {{#if center.tooltip}}data-tooltip="{{center.tooltip}}"{{/if}}></i>
        {{/if}}
    </div>
    {{/if}}

    {{#if trailing}}
    <div class="blacksmith-part-pip-group blacksmith-pip-trail blacksmith-pip-tone-{{trailing.tone}}">
        {{#each trailing.pips}}
        <span class="blacksmith-part-pip{{#if this}} blacksmith-pip-filled{{/if}}"></span>
        {{/each}}
    </div>
    {{/if}}

</div>
