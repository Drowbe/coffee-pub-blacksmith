{{!-- Banner or portrait, with optional caption and optional stacked overlays.
      Crier lays a blood layer over a character portrait, chosen by remaining hit
      points -- overlays are ordinary images drawn on top, not a special case. --}}
<div class="blacksmith-part-image">
    <div class="blacksmith-part-image-stack">
        <img src="{{src}}" alt="{{alt}}" />
        {{#each overlays}}
        <img class="blacksmith-part-image-overlay" src="{{this}}" alt="" />
        {{/each}}
    </div>
    {{#if caption}}<div class="blacksmith-part-image-caption">{{{caption}}}</div>{{/if}}
</div>
