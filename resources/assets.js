/**
 * Bundled asset data — re-exported from assets-legacy.js.
 * Run `node scripts/extract-assets-to-json.mjs` (from repo root) to emit JSON defaults under resources/asset-defaults/.
 * Per-category overrides: settings (Asset Mapping) — merge logic in asset-loader.js (future).
 */
export * from './assets-legacy.js';
